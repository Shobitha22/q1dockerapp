apiVersion: v1
kind: Service
metadata:
  name: q1dockerapp-service
spec:
  type: NodePort
  selector:
    app: q1dockerapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30082
